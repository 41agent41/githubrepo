# =============================================================================
# TradingApp - Minimal Configuration Template
# =============================================================================
#
# IMPORTANT: Most settings are now configurable via the browser UI!
#
#   - IB Connections: http://YOUR_SERVER_IP:3000/connections
#   - System Settings: http://YOUR_SERVER_IP:3000/settings
#
# This file only contains the MINIMUM required settings for initial startup.
# All other settings (CORS, Redis, Security, etc.) are stored in the database
# and can be modified through the browser without restarting services.
#
# =============================================================================

# ===== BOOTSTRAP CONFIGURATION (REQUIRED) =====
# These are the only settings that MUST be in this file

# Server IP for Docker networking
SERVER_IP=YOUR_SERVER_IP

# Service Ports (for Docker/networking)
FRONTEND_PORT=3000
BACKEND_PORT=4000
IB_SERVICE_PORT=8000

# PostgreSQL Database (required for storing all other settings)
POSTGRES_HOST=YOUR_DATABASE_HOST
POSTGRES_PORT=5432
POSTGRES_USER=tradingapp
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD
POSTGRES_DB=tradingapp
POSTGRES_SSL=true

# ===== OPTIONAL OVERRIDES =====
# These can also be configured via browser at /settings

# NODE_ENV=production
# FRONTEND_HOST=0.0.0.0
# BACKEND_HOST=0.0.0.0

# ===== BROWSER-CONFIGURABLE SETTINGS =====
# The following are NO LONGER NEEDED in this file.
# Configure them at http://YOUR_SERVER_IP:3000/settings
#
# CORS Settings:
#   - allowed_origins, allow_credentials, allowed_methods, allowed_headers
#
# Security Settings:
#   - jwt_secret (auto-generated if empty)
#   - session_secret (auto-generated if empty)
#   - jwt_expiry_hours, require_auth
#
# Redis Settings:
#   - redis host, port, password, enabled, ttl
#
# IB Connection Settings (at /connections):
#   - IB_HOST, IB_PORT, IB_CLIENT_ID, IB_TIMEOUT
#   - Support for multiple Gateway/TWS connections (Live/Paper)
#
# Timezone Settings:
#   - default_timezone, ib_timezone, data_timezone, date/time formats
#
# Data Collection Settings:
#   - auto_collect_enabled, collection_interval, default_symbols/timeframes
#
# UI Settings:
#   - theme, chart_type, auto_refresh, max_chart_bars
#
# API Settings:
#   - rate_limit_enabled, rate_limit_requests, request_timeout
#
# Feature Flags:
#   - enable_live_trading, enable_backtesting, enable_strategies, etc.
#
# Notification Settings:
#   - email_enabled, smtp configuration

# =============================================================================
# EXAMPLE - Minimal production .env file:
# =============================================================================
#
# SERVER_IP=10.7.3.20
# FRONTEND_PORT=3000
# BACKEND_PORT=4000
# IB_SERVICE_PORT=8000
# POSTGRES_HOST=db.example.com
# POSTGRES_PORT=5432
# POSTGRES_USER=tradingapp
# POSTGRES_PASSWORD=super_secure_password_123
# POSTGRES_DB=tradingapp
# POSTGRES_SSL=true
#
# That's it! Everything else is configured via browser.
# ============================================================================= 