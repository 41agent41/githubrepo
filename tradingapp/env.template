# =============================================================================
# TradingApp - MINIMAL BOOTSTRAP CONFIGURATION
# =============================================================================
#
# Copy this file to .env and update the values for your deployment:
#   cp env.template .env
#   nano .env
#
# IMPORTANT: This is the ONLY configuration file needed!
# All other settings are configured via browser after deployment:
#
#   - IB Connections:   http://YOUR_SERVER_IP:3000/connections
#   - System Settings:  http://YOUR_SERVER_IP:3000/settings
#
# =============================================================================

# ===== SERVER IP (REQUIRED) =====
# Your server's IP address for Docker networking
SERVER_IP=YOUR_SERVER_IP

# ===== SERVICE PORTS (REQUIRED) =====
# Ports for Docker container mapping
FRONTEND_PORT=3000
BACKEND_PORT=4000
IB_SERVICE_PORT=8000

# ===== DATABASE CONNECTION (REQUIRED) =====
# PostgreSQL database - needed to load browser-configurable settings
POSTGRES_HOST=YOUR_DATABASE_HOST
POSTGRES_PORT=5432
POSTGRES_USER=tradingapp
POSTGRES_PASSWORD=YOUR_DATABASE_PASSWORD
POSTGRES_DB=tradingapp
POSTGRES_SSL=true

# =============================================================================
# EXAMPLE - Replace placeholders with your actual values:
# =============================================================================
#
# SERVER_IP=10.7.3.20
# FRONTEND_PORT=3000
# BACKEND_PORT=4000
# IB_SERVICE_PORT=8000
# POSTGRES_HOST=db.example.com
# POSTGRES_PORT=5432
# POSTGRES_USER=tradingapp
# POSTGRES_PASSWORD=super_secure_password_123
# POSTGRES_DB=tradingapp
# POSTGRES_SSL=true
#
# =============================================================================
# BROWSER-CONFIGURABLE SETTINGS (NOT needed in this file)
# =============================================================================
#
# The following are configured via browser at /settings:
#   - CORS (allowed origins, methods, headers)
#   - Redis (host, port, password, caching)
#   - Security (JWT secret, session secret - auto-generated)
#   - Timezone (default, IB, data timezone, date/time formats)
#   - Data Collection (auto-collect, intervals, symbols, timeframes)
#   - UI (theme, chart type, auto-refresh)
#   - API (rate limiting, timeouts)
#   - Features (live trading, backtesting, strategies, alerts)
#   - Notifications (email/SMTP configuration)
#
# IB Gateway/TWS connections are configured via browser at /connections:
#   - Multiple connection profiles (Gateway/TWS, Live/Paper)
#   - Host, Port, Client ID per profile
#   - Connection testing and history
#
# =============================================================================
