# =============================================================================
# TradingApp - BOOTSTRAP: DATABASE ONLY
# =============================================================================
#
# Copy this file to .env and set ONLY the database connection:
#   cp env.template .env
#   nano .env
#
# Only PostgreSQL is required here (backend must connect to load other settings).
# All other configuration is in System Settings (browser):
#
#   - System Settings:  http://YOUR_SERVER_IP:3000/settings
#     → Deployment & URLs (server IP, ports, IB service URL, frontend URL)
#     → CORS, Redis, Security, Timezone, Data Collection, UI, API, Features, Notifications
#   - IB Connections:   http://YOUR_SERVER_IP:3000/connections
#
# =============================================================================

# ===== DATABASE CONNECTION (REQUIRED) =====
# PostgreSQL - required for backend to start and load System Settings from DB
POSTGRES_HOST=YOUR_DATABASE_HOST
POSTGRES_PORT=5432
POSTGRES_USER=tradingapp
POSTGRES_PASSWORD=YOUR_DATABASE_PASSWORD
POSTGRES_DB=tradingapp
POSTGRES_SSL=true

# ===== OPTIONAL: Override bind port if needed =====
# PORT=4000

# =============================================================================
# EXAMPLE
# =============================================================================
#
# POSTGRES_HOST=10.7.3.21
# POSTGRES_PORT=5432
# POSTGRES_USER=tradingapp
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_DB=tradingapp
# POSTGRES_SSL=true
#
# =============================================================================
# EVERYTHING ELSE → System Settings (http://YOUR_SERVER:3000/settings)
# =============================================================================
#
# In System Settings → Deployment & URLs you configure:
#   server_ip, frontend_port, backend_port, ib_service_port,
#   ib_service_url, frontend_url
# Plus CORS, Redis, Security, Timezone, Data Collection, UI, API, Features, Notifications.
# IB connections are configured at /connections.
#
# =============================================================================
